# Full pipeline reproduction (offline, with SeizeIT2 data on disk).
# These are the dependencies for src/ — the scripts that read .edf files,
# extract features, train models, and produce the artefacts in results/.
#
# For the interactive Streamlit demo only, see requirements.txt instead
# (it is intentionally lighter so the app deploys cleanly on Streamlit Cloud).
#
# Usage:
#     pip install -r requirements-pipeline.txt

# NumPy 1.x and 2.x both work — the src/ code has been audited for numpy 2.0
# breaking changes (no np.int/np.float/np.bool legacy aliases, no removed
# stable APIs). Keeping a wide range avoids painful downgrades on platforms
# (e.g. Google Colab) that already ship numpy 2.x and depend on it for
# jax/opencv/shap/tifffile.
mne>=1.6
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7

# Demo dependencies are also useful for local validation of the dashboard.
streamlit>=1.30
plotly>=5.18
