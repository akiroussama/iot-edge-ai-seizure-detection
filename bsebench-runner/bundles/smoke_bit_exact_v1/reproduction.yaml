bundle_id: smoke_bit_exact_v1
description: Self-contained smoke bundle proving bit-exact comparison logic.
reference_manifest: reference_manifest.json
output_root: outputs
commands:
  - ["python", "-c", "from pathlib import Path; Path('outputs').mkdir(exist_ok=True); Path('outputs/result.txt').write_bytes(b'bsebench smoke\\n')"]
